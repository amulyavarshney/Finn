# Resolve against the public registry. Without this the lockfile inherits
# whatever registry the machine is configured for, and a host-specific mirror
# makes `npm ci` unreachable from CI and from Vercel's build machines.
registry=https://registry.npmjs.org/
